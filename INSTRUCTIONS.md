# Instructions to run your benchmark

By default, `make build` builds all of the files in your current directory with the `.hip` extension and places the binaries into the bin directory. To run simulations, execute `make run` in your terminal.